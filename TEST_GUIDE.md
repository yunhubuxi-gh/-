# docx（Word）文档处理测试指南

> 日期：2026-08-16
> 范围：验证修复后的 docx 文档完整处理流水线（正文 + 图片提取 + 图片 OCR 统一分片 + 图片多模态向量化）。
> 前置：后端已完成迁移与依赖安装；测试账号 `admin / Teacher@123`（owner，可上传）。

## 环境准备

```bash
# 1) 迁移（幂等）
python scripts/migrate_course.py

# 2) 启动后端
python -X utf8 -m uvicorn api.main:app --host 0.0.0.0 --port 8000

# 3) 启动前端
python -X utf8 -m streamlit run frontend/app.py --server.port 8501
```

- 教师账号：`admin / Teacher@123`（owner 权限，可上传/出卷）
- `.env` 关键开关：`ENABLE_IMAGE_EMBED`（图片向量化总开关）、`IMAGE_EMBED_PROVIDER`（`local` / `volcano`）

---

## 测试用例 1 — 含多图 docx：正文分块 + 图片 OCR 文本入库 + 图片向量化

**目的**：验证 docx 修复后走完整流水线——正文与图片 OCR 文本统一分块入库，图片同时产生多模态向量；上传成功。

**步骤**：
1. `.env` 设 `ENABLE_IMAGE_EMBED=true`，重启后端。
2. 准备一个含多张内嵌图片（如数学公式/习题截图，图片内文字清晰）的 docx，正文放若干段落 + 一个表格（表格内含文字与图片）。
3. 前端「文档管理」→ 选择课程库 → 上传该 docx。
4. 观察进度条依次经过：`解析文件 → 提取页面图片 → OCR文字识别 → 文本分块&文本向量化 → 图片预处理 → 图片多模态Embedding向量化 → 写入向量库完成`。
5. 完成后文档状态为「就绪」，无警告。

**后端日志预期**：
```
DOCX 解析完成: 提取到 N 张内嵌图片, pages=M, chars=...
```

**通过标准**：
- 文档最终 `ready`（非 `failed`）。
- 正文文本 + 每张图片的 OCR 文字**都**进入向量库（可用图片内文字提问命中，例如图片里写「二次函数顶点公式」，检索「二次函数顶点」能命中该文档）。
- 表格内文字与表格内图片均被提取，不遗漏。
- 图片产生 CLIP/豆包 多模态向量（图片检索分支可召回）。

---

## 测试用例 2 — docx 含损坏图片：整体成功，仅跳过该图

**目的**：验证异常隔离铁则——单张图片损坏/OCR 失败不导致 docx 上传任务失败。

**步骤**：
1. `.env` 设 `ENABLE_IMAGE_EMBED=true`。
2. 准备一个 docx：正文正常 + 1 张损坏图片（人为破坏图片字节，使 PIL 无法解码）+ 1 张正常图片。
3. 上传该 docx。
4. 观察文档状态与前端提示。

**后端日志预期**：
```
DOCX 解析完成: 提取到 2 张内嵌图片, ...   ← 损坏图仍被提取
docx 图片 OCR 失败（跳过该图 OCR，不影响整体）: ...   ← 损坏图 OCR 优雅降级
[图片向量化] 图片 page_x_img_y 失败: <原因>   ← 仅损坏图预处理/向量化失败
[图片向量化] 成功 1 张，失败 1 张，文本内容已正常入库
```

**通过标准**：
- 文档最终 `ready`（非 `failed`）。
- 正文文本 + 正常图片的 OCR 文本照常入库。
- 仅损坏图被跳过，其余图片向量 + 文本向量全部完成。
- 前端展示 ⚠️ 警告而非报错。

---

## 测试用例 3 — 切换多模态后端（local ↔ volcano）

**目的**：验证 docx 图片向量化在两套后端间可无缝切换，切换后 docx 上传仍正常。

**步骤**：
1. 切换 A：`.env` 设 `IMAGE_EMBED_PROVIDER=local`（Chinese-CLIP），重启后端，上传含图 docx，确认成功。
2. 切换 B：`.env` 设 `IMAGE_EMBED_PROVIDER=volcano`（豆包 `doubao-embedding-vision-251215`，Agent Plan 专属地址），重启后端，上传同一 docx，确认成功。

**后端日志预期**（volcano）：
```
[图片向量化] 使用豆包多模态 Embedding（dim=...）
```

**通过标准**：
- 两种 provider 下 docx 上传均 `ready`，正文/图片 OCR 分块不受影响。
- 图片向量维度隔离正确（`local` 768 维 / `volcano` 1024 或 2048 维，分别落到 `kb_{id}_img_{dim}` 集合，互不冲突）。
- 豆包 key/地址均从 `.env` 读取，无硬编码。

---

## 测试用例 4 — `ENABLE_IMAGE_EMBED=false`：跳过图片处理，文本正常分块入库

**目的**：验证关闭开关后 docx 只走文本链路，不做图片落盘/向量化，正文 + 图片 OCR 文本仍正常分块入库。

**步骤**：
1. `.env` 设 `ENABLE_IMAGE_EMBED=false`，重启后端。
2. 上传含图 docx。
3. 观察进度条**无**「图片预处理 / 图片多模态Embedding向量化」子状态。
4. 完成后文档 `ready`。

**后端日志预期**：无 `image_preprocess` / `image_embedding` / `[图片向量化]` 相关日志。

**通过标准**：
- 文档最终 `ready`，无图片向量化日志。
- 正文文本正常分块入库，文本检索可命中。
- 启动不报错（即使 `torch/transformers/modelscope` 未安装）。

---

## 测试用例 5 — PDF 回归：原流程不受影响

**目的**：验证本次仅改 docx 解析器，PDF 解析/分块/向量写入完全不受影响。

**步骤**：
1. `.env` 设 `ENABLE_IMAGE_EMBED=true`。
2. 上传一个含图 PDF（原生 + 扫描均可）。
3. 观察 PDF 仍走原有完整流程：`解析文件 → 提取页面图片 → OCR文字识别 → 文本分块&文本向量化 → 图片预处理 → 图片多模态Embedding向量化 → 写入向量库完成`。
4. 完成后文档 `ready`。

**通过标准**：
- PDF 行为与此前完全一致，无回归。
- 底层 `pdf_parser.py` / 分块器 / `document_service` / `rag_pipeline` 未被改动（可用 `git diff` 确认本次仅 `ai/rag_engine/document_parser/docx_parser.py` 变更）。

---

## 回归验证（原有业务不破坏）

```bash
python tests/test_step1_utils.py     # 工具/配置
python tests/test_step2_db.py        # 数据模型
python tests/test_step3_rag.py       # RAG（BM25+向量+rerank，未动）
python tests/test_step5_services.py  # 服务层
python tests/test_step6_api.py       # API
```

**通过标准**：全部绿灯；本次仅 docx 解析器变更，底层未动，测试结果与此前一致。

---

# 附：三大痛点优化测试用例

> 日期：2026-08-16
> 范围：验证 OCR 并发、细粒度进度、本地 CLIP 内存回收、OCR 退避重试等工程优化。

## 测试 1 — 大量图片 docx：OCR 进度 + 处理提速

**目的**：验证 docx 大量内嵌图片时，前端展示「OCR文字识别中 x/总数」进度，且处理速度对比优化前提升。

**步骤**：
1. `.env` 设 `OCR_CONCURRENCY=4`（默认），重启后端。
2. 上传含大量内嵌图片的 docx（如 50+ 张截图习题）。
3. 观察前端上传进度条：解析文件 → 提取图片 → **`OCR文字识别中：x / 56 张`（数字实时递增）** → 文本分块 → 多模态向量化 → 写入向量库完成。

**后端日志预期**：
```
DOCX 解析完成: 提取到 56 张内嵌图片, pages=..., chars=...
```

**通过标准**：
- 前端进度条不再一直停在「解析中」，OCR 阶段能看到 `x / 总数` 实时递增。
- 处理总耗时明显缩短（并发 4 进程下，56 张图由约 3 分钟降至约 1 分钟，视机器而定；首次上传含进程池冷启动开销，后续复用更快）。
- 文档最终 `ready`，正文 + 图片 OCR 文本正常分块入库。

## 测试 2 — local 本地 CLIP 连续多图：内存不无限上涨

**目的**：验证本地 Chinese-CLIP 模式连续处理多张图片时，内存不会无限持续上涨。

**步骤**：
1. `.env` 设 `ENABLE_IMAGE_EMBED=true`、`IMAGE_EMBED_PROVIDER=local`、`CLIP_GC_INTERVAL=10`，重启后端。
2. 连续多次上传含大量图片的文档（或单个含几十张图的文档）。
3. 用任务管理器观察 python 进程内存占用。

**后端日志预期**：无 OOM；图片向量化正常完成。

**通过标准**：
- 每完成一批图片推理后内存有回落（`gc.collect()` 生效），进程内存曲线呈锯齿状而非单调上涨至崩溃。
- 图片向量化全程完成，文档 `ready`，无 OOM 崩溃。

## 测试 3 — OCR 初始化临时失败：自动恢复 + 前端告警

**目的**：验证 OCR 初始化失败后不会永久僵死，退避到期自动恢复，前端有故障提示。

**步骤**：
1. 模拟 OCR 初始化失败（任选一种）：
   - 临时将 `.env` 的 `OCR_ENGINE` 改成一个不存在的值（如 `paddleocr_bad`），重启后端；
   - 或临时移除 paddleocr/paddlepaddle 依赖使其 import 失败。
2. 上传含图片 docx，观察：
   - 后端日志出现 `OCR 引擎初始化失败（第 1 次）...将在 60 秒后自动重试`；
   - 文档完成后前端出现 ⚠️ 警告「OCR 文字识别不可用（...），图片内文字已跳过」。
3. 恢复 OCR 可用（改回 `OCR_ENGINE=paddleocr` 或装回依赖），**无需重启**，等待退避时间（`OCR_RETRY_INTERVAL`，默认 60s）后再次上传含图 docx。
4. 观察 OCR 自动恢复，图片文字正常识别入库。

**后端日志预期**：
```
OCR 引擎初始化失败（第 1 次）: ...，将在 60 秒后自动重试
OCR 引擎初始化失败（第 2 次）: ...，将在 120 秒后自动重试   ← 指数退避
```

**通过标准**：
- 失败后不崩溃、不永久禁用；退避期内不重复初始化刷日志。
- 前端有 OCR 失败告警（非静默跳过）。
- 恢复后无需重启服务，OCR 自动重新可用。

## 测试 4 — 原有功能回归：volcano / PDF / 出卷 / 登录

**目的**：验证本次优化未破坏原有业务。

**步骤**：
1. `.env` 设 `IMAGE_EMBED_PROVIDER=volcano`（填豆包 Key），重启后端，上传含图 PDF → 图片向量化正常。
2. 上传普通文本 PDF → 文本分块 + 检索正常。
3. `admin` 登录 → 生成试卷 → 双 Agent 命题校验正常。
4. 登录系统（admin/demo）正常。

**通过标准**：
- volcano 云端模式、PDF 上传、生成试题、登录系统全部功能不受影响。
- `python tests/test_step1_utils.py` ~ `test_step6_api.py` 全部绿灯（底层未动）。

---

# 附：上层业务迭代测试用例（去重/均衡 · 编辑模式 · 精细化批改）

> 日期：2026-08-16
> 范围：验证三大新功能：①试卷题目去重 + 知识点均衡校验；②试卷编辑模式；③精细化溯源批改。
> 前置：后端已完成迁移与依赖安装；测试账号 `admin / Teacher@123`（owner，可上传/出卷）。

## 测试 1 — 重复题目自动检测并重出

**目的**：校验 Agent 第 5 组校验（相似度去重）能识别试卷内近似重复题，并自动重出。

**步骤**：
1. `.env` 确认 `EXAM_DUP_SIMILARITY_THRESHOLD=0.9`（默认）。
2. 生成一份含 10 题的试卷（选择/填空/简答）。
3. 在后端日志 / 前端「双 Agent 执行轨迹」中查看校验结果：
   - 若 LLM 恰好出了 2 道近似重复题（题干+知识点相似度 ≥ 0.9），会看到该校验轮出现 `rule: dedup` 的不合格记录，reason 为「题目与试卷内其它题文本相似度过高…」；
   - 系统自动把重复题回传命题 Agent 重出，最终试卷无重复题。

**后端日志预期**：
```
校验评审 Agent 完成: 第N轮, 校验=10题, 不合格=X题（含去重/偏科 Y 题）
```

**通过标准**：
- 去重判定的不合格题带 `rule: dedup`，其 reject_reason 回传给命题 Agent 并被重出；
- 最终 `ready` 试卷内任意两题文本相似度 < 阈值（可用纯 Python 相似度自测验证）。

## 测试 2 — 知识点偏科自动重出，卷面考点均衡

**目的**：校验 Agent 第 5 组校验（知识点均衡）能把占比过高的考点打回重出，使整套试卷考点分布均衡。

**步骤**：
1. `.env` 确认 `EXAM_KNOWLEDGE_MAX_RATIO=0.5`（默认）。
2. 生成试卷；若某个知识点在卷面中占比 > 50%，会看到该校验轮出现 `rule: balance` 的不合格记录，reason 为「知识点『X』占比过高（…）超过上限 50%，判定偏科」。
3. 系统自动把该知识点下超出的题回传命题 Agent，**换用其它课件原文考点**重新出题。

**通过标准**：
- 偏科判定的不合格题带 `rule: balance` 且明确给出占比；
- 最终试卷任意知识点题数占比 ≤ `EXAM_KNOWLEDGE_MAX_RATIO`；
- 前端试卷详情「知识点分布」标签云 / 雷达图可见考点分散、无单点占比过高。

## 测试 3 — 编辑模式：单题重出 / 编辑 / 删除 / 新增，雷达图同步刷新

**目的**：验证试卷编辑闭环与知识点可视化的联动刷新。

**步骤**：
1. 以 `admin` 打开「试卷管理」→ 展开一份 `ready` 试卷。
2. 打开 **✏️ 编辑模式** 开关：
   - **单题重出**：点某题「🔁 重出」→ 等待命题 Agent 重出（约 10~30s）→ 该题被替换，其余题不动；
   - **编辑题目**：点「✏️ 编辑」→ 修改题干/选项/参考答案/知识点/分值 → 保存；
   - **删除题目**：点「🗑 删除」→ 该题移除，总分同步重算；
   - **新增题目**：展开「➕ 新增自定义试题」→ 填题型/题干/选项/答案/知识点/分值 → 添加。
3. 每次操作后观察试卷详情页的**知识点标签云 + 覆盖率雷达图**是否随题库实时刷新。
4. 刷新页面确认修改已持久化（写入数据库）。

**通过标准**：
- 单题重出不改动其它题（题目数不变、qid 稳定）；
- 编辑/删除/新增后总分、题型配置正确重算（服务端自动重算并覆盖）；
- 每次编辑后标签云 / 雷达图自动更新；
- 数据持久化，刷新后仍在；read 学生看不到编辑模式。

## 测试 4 — 客观题错误解析 + 文档溯源片段

**目的**：验证答错的客观题（单选/填空）展示「错误解析 + 考察知识点 + 课件原文溯源」。

**步骤**：
1. 学生（read）选择一份含选择题的 `ready` 试卷，故意把某道单选题答错提交。
2. 前端自动轮询批改状态（统一后台批改），完成后在「在线答题」或教师「批改与成绩」中查看该题：
   - 显示 `❌ 错误解析`（为什么所选选项不对）；
   - 显示 `🎯 本题考察知识点`；
   - 显示 `📖 课件原文溯源`（来自该题生成时的逐字摘录或 RAG 检索片段）。
3. 答对的选择题不生成错误解析（省成本）。

**通过标准**：
- 答错客观题有错误解析 + 知识点 + 溯源片段；正确题正常显示 `✅ 正确`；
- 溯源片段与课件原文逐字一致（防幻觉锚定）；
- 全客观题试卷也会进入后台批改并展示上述明细。

## 测试 5 — 主观题四维度分项打分 + 分项溯源

**目的**：验证简答题按四维度分项打分，每维度独立点评 + 独立课件溯源。

**步骤**：
1. 学生提交一份含简答题的答卷（答案覆盖知识点但步骤/表述有欠缺）。
2. 批改完成后查看该简答题：
   - 显示 `📐 四维度分项打分`：① 知识点匹配度 ② 答题步骤完整性 ③ 结论答案正确性 ④ 语言表述规范性；
   - 每个维度有 `得分/满分`、独立 `comment` 点评、对应的 `📖 课件原文溯源`；
   - 上方总分 = 客观分 + 主观分（四维度得分之和）。
3. 修改 `.env` 的 `EXAM_GRADE_WEIGHT_*`（如改为 40/30/20/10）重启后端，再次提交，确认各维度满分按新权重分配。

**通过标准**：
- 四维度均显示得分 + 点评 + 溯源原文（≥ 能支撑该维度评判的逐字摘录）；
- 各维度满分之和 = 该题满分；
- 权重可配置且生效（重启后按新权重拆分）。

## 测试 6 — 原有功能回归

**目的**：验证本次上层迭代不破坏原有全部功能。

**步骤**：
1. `admin` 登录系统（鉴权正常）。
2. 上传含图 PDF / docx → 文档解析 → 文本 + 图片多模态向量化正常（含 OCR 并发、进度反馈）。
3. 生成试卷（双 Agent 出卷）→ 原 4 项校验 + 轨迹正常。
4. 学生在线答题、客观题规则判分、教师查看全班答卷正常。
5. 导出 Markdown（含/不含答案）正常。
6. `.env` 关闭 `ENABLE_IMAGE_EMBED` 后系统正常退回纯文本 RAG。

**通过标准**：
- 所有老功能行为与此前一致，无回归；
- `python tests/test_step1_utils.py` ~ `test_step6_api.py` 全部绿灯；
- 底层 RAG / 文档解析 / 多模态双后端 / 异步任务 / 鉴权 / 数据库旧表未改动（`git diff` 仅上述 9 个业务文件 + 2 个文档）。
