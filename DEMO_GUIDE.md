# 企业私有知识库智能助手 · 答辩演示操作手册（DEMO_GUIDE）

> 本手册用于毕业设计答辩现场演示。每一步都明确「你做什么操作」→「前端页面出现什么肉眼可见变化」→「证明哪项核心能力」。
> 全程**不修改任何源码 / 配置 / 部署脚本**，只使用本仓库提供的演示素材，手动在网页上操作即可看到全部交互变化。

---

## 零、演示前的准备（重要，务必先做）

### 0.1 环境前提

1. 后端已能启动（`python -X utf8 -m uvicorn api.main:app --host 0.0.0.0 --port 8000`）。
2. 前端已能启动（`python -X utf8 -m streamlit run frontend/app.py --server.address 0.0.0.0 --server.port 8501 --server.headless true`）。
3. 浏览器打开前端地址：**http://localhost:8501**

### 0.2 关键前置条件（影响问答 / Agent / 文档向量化能否出真实结果）

> ⚠️ 演示"RAG 问答""Agent 任务""文档向量化完成"这三个环节，依赖两项真实能力，请提前确认：

1. **大模型 API Key**：编辑项目根目录 `.env`，把 `LLM_API_KEY` 换成真实可用的 Key（如 DeepSeek `sk-xxx`），并确认 `LLM_MODEL`、`LLM_BASE_URL` 正确。
   - 否则问答、Agent 会因调用大模型失败而无结果。
2. **嵌入模型可用**：`EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5`，文档向量化需要加载 BGE 嵌入模型（首次运行会联网下载，或需已缓存）。
   - 否则文档上传后状态会停在「失败」而非「就绪」，检索也会无结果。

> 验证方法：先上传 1 个文档，看它能否从「待解析」走到「就绪」；再在问答页问一句，看是否返回带引用的答案。通过后再开始正式演示。

### 0.3 演示账号

| 账号 | 密码 | 角色 | 用途 |
|------|------|------|------|
| `admin` | `admin123456` | 系统管理员 | 主演示账号：建库、传文档、问答、Agent、加成员 |
| `demo` | `demo123456` | 普通用户 | 权限演示账号：验证只读权限隔离 |

> 两个账号均已由系统初始化脚本自动创建。`demo` 用户的 **user_id 默认为 2**（`admin` 为 1），第 8 步添加成员时会用到。

### 0.4 演示素材位置

```
demo_docs/
├── RAG技术原理简介.md
├── LangGraph Agent工作机制.md
├── 混合检索BM25+向量+重排序说明.md   ← 这 3 份上传到「AI技术知识库」
└── 公司内部简易规章制度.txt          ← 这份上传到「公司制度知识库」
```

---

## 一、完整演示流程（照步骤依次操作）

---

### 步骤 1：启动并打开登录页

**操作**：启动后端、前端，浏览器打开 http://localhost:8501

**前端现象**：
- 页面呈现**全屏深色渐变背景**（深蓝渐变），中间是一张**居中的白色大卡片**，卡片带明显阴影与圆角。
- 卡片顶部是「企业私有知识库智能助手」标题 + 副标题「企业级 · 私有化 · RAG 检索增强生成平台」。
- 卡片内有「登 录」「注 册」两个标签页，账号、密码输入框**背景为浅灰色、边框清晰可见、输入文字为黑色**。

**证明能力**：前后端分离架构、登录页已按高对比度 UI 规范重做（根治白底白字）。

---

### 步骤 2：使用 admin 登录

**操作**：在登录页输入 `admin` / `admin123456`，点击「登 录」按钮。

**前端现象**：
- 右上角弹出「✅ 欢迎回来，系统管理员」提示。
- 页面**自动跳转到「知识库管理」页面**（而非问答页）。
- 左侧出现**常驻侧边栏**：顶部品牌 Logo、**当前用户名「系统管理员」+ 角色「管理员」卡片**、五个导航项（知识库管理 / 文档管理 / 智能问答 / Agent 任务 / 审计日志）、底部「🚪 退出登录」按钮。
- 因为此时还没有知识库，主区域显示一张**空状态引导大卡片**，中间一个醒目的蓝色按钮「＋ 新建第一个知识库」。

**证明能力**：JWT 鉴权、登录成功默认落地知识库页、侧边栏用户名展示与登出、空状态引导。

---

### 步骤 3：创建两个知识库

**操作**：
1. 点击空状态卡片上的「＋ 新建第一个知识库」，在表单中填写名称 `AI技术知识库`，点击「创建」。
2. 创建成功后，在右侧「新建知识库」卡片中再填名称 `公司制度知识库`，点击「创建」。

**前端现象**：
- 每次点击「创建」，右上角弹出「✅ 知识库创建成功」，页面刷新。
- 主区域左侧**卡片列表依次新增知识库卡片**，每张卡片显示：知识库名称、角色徽章（所有者，紫色）、描述、📄 文档数、🧩 分块数、状态徽章。
- 页面下方出现「🔧 知识库详情与成员」区，可下拉选择任意知识库查看详情。

**证明能力**：知识库 CRUD、卡片化列表、四级 RBAC 中 owner 角色。

---

### 步骤 4：上传演示文档（异步向量化）

**操作**：点击左侧导航「📄 文档管理」，下拉选择「AI技术知识库」，在上传卡片中依次上传 `demo_docs/` 下的 3 份 md 文档（每次点「上传并向量化」）。

**前端现象**：
- 点「上传并向量化」后，右上角弹出「✅ 上传成功，开始向量化（task_id: xxx）」。
- 页面出现一条**实时进度条**，进度条文字随状态实时刷新：`状态：uploaded → parsing → parsed → embedding → ready`。
- 处理完成时进度条跳满，文字变为「向量化完成 ✅」，右上角弹出「文档已就绪，可检索」。
- 页面刷新后，下方「文档列表」表格出现新行，每行含 ID、标题、类型、大小、**状态徽章（绿色「就绪」）**、分块数、版本号。

**证明能力**：文档异步上传 + 任务进度轮询 UI（非阻塞）、三阶段文档处理流水线可视化。

---

### 步骤 5：进入 RAG 问答页，选中知识库

**操作**：点击左侧导航「💬 智能问答」。

**前端现象**：
- 顶部出现「选择知识库」「选择会话」两个下拉框。
- 下拉选择「AI技术知识库」后，下方**出现聊天输入框**（底部 "请输入你的问题，回车发送…"）。
- （对照说明：若此时没有知识库，页面不会出现输入框，而是显示一张引导大卡片 + 「去创建知识库」按钮——这正是"无知识库时禁止无效输入"的兜底。）

**证明能力**：问答页前置条件检测、知识库选择、会话管理。

---

### 步骤 6：RAG 问答功能演示

**操作 6a**：在输入框提问「**RAG 的核心流程是什么？**」，回车。

**前端现象**：
- 右侧出现一条**蓝色用户气泡**（你的问题）。
- 紧接着出现「正在检索并生成回答…」加载提示，随后**流式打字机效果**逐字输出回答气泡。
- 回答气泡下方出现一张**引用来源卡片**，逐条列出 `[1] 文档名 · 第X页 · 块Y`，标明答案来自哪篇文档的哪个分块。

**证明能力**：RAG 检索增强生成、引用溯源、流式输出体验。

**操作 6b**：再提问一个知识库中**不存在**的虚构问题，例如「**公司组织团建旅游的流程是什么？**」，回车。

**前端现象**：
- 同样出现用户气泡 + 回答气泡。
- 模型**如实回答"知识库中没有相关文档/没有相关资料"**，而不是编造内容，气泡正常渲染，无报错。

**证明能力**：幻觉抑制、无答案识别，回答"有据可查、不胡说"。

---

### 步骤 7：Agent 智能任务演示

**操作**：点击左侧导航「🤖 Agent 任务」，选择「AI技术知识库」，任务标题填 `AI知识库要点总结`，任务描述填「**总结全部 AI 知识库文档的要点，并对比 RAG 和 Agent 的区别**」，点击「提交 Agent 任务」。

**前端现象**：
- 右上角弹出「✅ 任务已提交：<task_id>」。
- 出现一条**任务进度条**，状态实时刷新：`任务状态：pending → planning → executing → reflecting → success`。
- 成功后进度条跳满，文字「任务完成 ✅」。
- 页面刷新，下方「任务历史」出现一条**可展开的任务卡片**（含状态徽章 + 重试次数）。
- 点开卡片，可**可视化查看完整执行轨迹**：📋 任务规划（拆解步骤 + 工具）、⚙️ 执行日志（每步工具 + 状态 + 耗时）、🔁 反思记录（重试原因与修正策略）、最终执行结果。

**证明能力**：LangGraph Agent 任务拆解、工具调用、反思重试全链路可视化。

---

### 步骤 8：权限隔离演示（RBAC）

**操作**：
1. 点击导航「📚 知识库管理」，下方「🔧 知识库详情与成员」下拉选择「公司制度知识库」。
2. 在右侧「成员管理」→「添加成员」中，用户ID 填 `2`，角色选「只读」，点击「添加」。
3. 页面左上角侧边栏点击「🚪 退出登录」，回到登录页。
4. 用 `demo` / `demo123456` 登录。

**前端现象**：
- 添加成员后，成员列表中出现「演示用户」+「只读」徽章，右上角弹出「✅ 成员已添加」。
- demo 登录后同样**自动跳转知识库管理页**，能看到「公司制度知识库」卡片（角色徽章变为「只读」，灰色）。
- 点击「📄 文档管理」并选择「公司制度知识库」：**上传文档的区块不显示**，替换为一条提示「你当前为只读成员，无上传文档权限」。
- 回到「📚 知识库管理」查看该知识库详情：成员管理区显示「你当前为只读成员，无成员管理权限」。

**证明能力**：四级 RBAC 权限隔离（owner/admin/write/read），前端按角色动态隐藏越权入口并给出友好提示。

---

### 步骤 9：审计日志查看

**操作**：用 demo 账号点击导航「📊 审计日志」。

**前端现象**：
- 顶部是筛选区（操作类型 / 结果 / 资源类型 / 页码）。
- 下方表格**逐行展示刚才全部操作记录**：登录、创建知识库、添加成员、上传文档、问答、Agent 任务等，每条含时间、用户ID、操作名称（中文）、结果徽章（成功/失败/越权拒绝）、资源类型与详情。
- 可通过「操作类型」下拉筛选只看「上传文档」或「问答」，也可切换「页码」分页浏览。

**证明能力**：全链路审计日志、只读查询、分页与多维筛选。

---

## 二、可选加分演示（越权拦截 + 审计落库）

> 展示"越权操作被后端 403 拦截并写入审计"的能力。用只读的 `demo` 账号直接调用上传接口（演示后端权限拦截，不改任何代码）。

**操作**（另开终端执行）：
1. 先登录 demo 拿 token：
   ```bash
   TOKEN=$(curl -s -X POST http://127.0.0.1:8000/api/v1/auth/login \
     -H "Content-Type: application/json" \
     -d '{"username":"demo","password":"demo123456"}' | python -c "import sys,json;print(json.load(sys.stdin)['data']['access_token'])")
   ```
2. 用 demo 的 token 越权向「公司制度知识库」上传文档（demo 只有 read 权限）：
   ```bash
   curl -s -X POST "http://127.0.0.1:8000/api/v1/kb/2/documents" \
     -H "Authorization: Bearer $TOKEN" \
     -F "file=@demo_docs/公司内部简易规章制度.txt"
   ```

**前端现象**：
- 终端返回 **HTTP 403**，响应体含统一错误码与「权限不足」消息。
- 回到前端「📊 审计日志」页刷新，表格中**新增一条结果徽章为「越权拒绝」的记录**（操作=上传文档，结果=permission_denied）。

**证明能力**：service 层权限拦截 + 越权审计双写。

---

## 三、答辩讲解要点速览

| 演示环节 | 一句话结论 |
|---------|-----------|
| 登录 → 知识库页 | JWT 鉴权 + 登录默认落地 + 高对比度 UI |
| 建库 → 传文档 | 异步任务进度条（非阻塞上传）|
| 问答 + 引用 | RAG 检索增强生成 + 引用溯源 + 幻觉抑制 |
| Agent 轨迹 | LangGraph 规划-执行-反思闭环可视化 |
| demo 只读 | 四级 RBAC，前端动态隐藏越权入口 |
| 审计表格 | 全链路操作留痕、可筛选分页 |

## 四、常见问题排查

1. **问答/Agent 无结果** → 检查 `.env` 中 `LLM_API_KEY` 是否为真实 Key。
2. **文档状态停在「失败」** → 检查 BGE 嵌入模型是否已下载/可用，看后端日志中的报错。
3. **添加成员报"用户不存在"** → 确认用户ID 填的是 `2`（demo）；若数据库不是全新初始化，可在审计日志或数据库中确认 demo 的实际 user_id。
4. **端口冲突** → 更换 `--port` 并同步 `.env` 的 `FRONTEND_API_BASE_URL`。

---

*本手册配套素材见 `demo_docs/` 目录，全部为新增演示文件，未改动项目任何源码、配置或部署脚本。*
