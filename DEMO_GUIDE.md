# 课程试卷智能命题校验批改系统 · 答辩演示操作手册（DEMO_GUIDE）

> 本手册用于毕业设计答辩现场演示，每一步都写清「你做什么操作」→「前端页面出现什么肉眼可见变化」→「证明哪项核心能力」。
> 全程**不修改任何源码**，只做网页操作与演示前的数据/账号准备。

---

## 零、演示前的准备（务必先做）

### 0.1 环境前提

1. 后端启动：`python -X utf8 -m uvicorn api.main:app --host 0.0.0.0 --port 8000`
2. 前端启动：`python -X utf8 -m streamlit run frontend/app.py --server.address 0.0.0.0 --server.port 8501 --server.headless true`
3. 浏览器打开：**http://localhost:8501**

### 0.2 关键前置（影响出题 / 批改是否出真实结果）

1. **大模型 Key**：`.env` 的 `DEEPSEEK_API_KEY` 为真实可用 Key（当前 `LLM_MODEL=deepseek-v4-flash`）。
   - 否则命题 / 校验 / 批改会因调用失败而无结果。
2. **嵌入模型可用**：`EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5`，课件向量化需要本地 BGE 模型（首次运行联网下载，或已缓存）。
   - 否则课件上传后状态停在「失败」，检索无结果。
3. **图片向量化开关**：`ENABLE_IMAGE_EMBED` 可关（默认已关），关闭时系统正常，仅图片检索不可用——这正可作「关闭开关不报错」的演示点。

> 验证方法：先上传 1 个课件，看它能否从「待解析」走到「就绪」；再进入「试卷中心」生成一次试卷，看是否能出题。通过后再开始正式演示。

### 0.3 演示账号（角色对应系统四级 RBAC）

| 账号 | 密码 | 课程库角色 | 用途 |
|------|------|-----------|------|
| `admin` | `Teacher@123` | owner（教师） | 主演示：建课程库、传课件、生成试卷、查看全班批改 |
| `demo` | `Student@123` | read（学生） | 学生演示：在线答题、查看自己批改结果 |

> 说明：`admin` 是系统管理员，同时也是自建课程库的 owner。`demo` 是普通用户，被教师加为某课程库的 read（只读/学生）成员。
> 若使用**全新数据库**，请先：① 在登录页注册两个账号；② 用教师账号建课程库；③ 在「课程库详情与成员」把学生账号（按用户 ID）加为「只读」成员。加成员需用户 ID，可在审计日志或数据库确认。

### 0.4 演示素材位置

```
demo_docs/
├── RAG技术原理简介.md
├── LangGraph Agent工作机制.md
├── 混合检索BM25+向量+重排序说明.md   ← 上传到「课程库」作课件素材
└── 公司内部简易规章制度.txt
```

> 当前数据库已预置「RAG优化自测库V2」课程库（已含课件）+ 一份 35 分、含 1 道简答题的已就绪试卷（paper 2），可**直接跳到步骤 5** 演示答题与批改，再从零演示建库出卷。

---

## 一、完整演示流程（照步骤依次操作）

---

### 步骤 1：登录（教师）

**操作**：输入 `admin` / `Teacher@123`，点「登 录」。

**前端现象**：
- 右上角弹出「✅ 欢迎回来」提示，页面自动跳到「📚 课程库」页。
- 左侧常驻侧边栏：顶部品牌「智能命题批改系统 / Exam Generation & Grading」、当前用户卡片、导航：**课程库 / 课件管理 / 试卷中心 / 智能问答 / Agent 任务 / 审计日志**。

**证明能力**：JWT 鉴权、登录默认落地课程库页、系统已从「知识库」更名为「课程库」。

---

### 步骤 2：创建课程库（带课程标签）

**操作**：点「＋ 新建第一个课程库」，填课程名称「数据结构」，课程描述「期末复习用课件」，课程标签输入 `算法,图论`，点「创建」。

**前端现象**：
- 弹出「✅ 课程库创建成功」，页面刷新。
- 出现课程库卡片，卡片展示：**课程名、描述、标签（算法 / 图论 两个小徽章）、📄 文档数、🧩 分块数、状态徽章、角色徽章（所有者）**。

**证明能力**：知识库→课程库语义改造、`tags` 标签字段、RBAC owner 角色。

---

### 步骤 3：上传课件（异步向量化，展示分阶段状态）

**操作**：点导航「📄 课件管理」，选「数据结构」课程库，上传 `demo_docs/` 下 1 份 md 课件，点「上传并向量化」。

**前端现象**：
- 右上角弹出「✅ 上传成功，开始向量化（task_id: xxx）」。
- 出现**实时进度条**，文字随状态刷新：`解析文件 → OCR识别（如需要）→ 文本分块向量化 → 写入向量库完成`（即 `uploaded → parsing → parsed → embedding → ready`）。
- 完成后进度条跳满「向量化完成 ✅」，文档列表出现新行，状态徽章为绿色「就绪」。

**证明能力**：文档上传异步任务、前端进度轮询 UI（不阻塞浏览器）、扫描件 OCR 通道、三阶段处理流水线可视化。

---

### 步骤 4：生成试卷（双 Agent 出卷 + 看完整执行轨迹）★核心★

**操作**：点导航「📝 试卷中心」→「试卷管理」标签页。选课程库「数据结构」，单选题 5、填空题 3、简答题 2，难度选「中」，点「生成试卷」。

**前端现象**：
1. 弹出「✅ 试卷生成任务已提交（ID xxx）」，出现进度条「双 Agent 出卷中（命题 → 逐题校验 → 重生成）…」。
2. 完成后弹出「试卷『数据结构试卷-中等-日期』已生成」。
3. 下方「历史试卷」出现一张可展开卡片，点开后：
   - 逐题显示题目（类型、分值、题干、选项、**答案、知识点、来源引用**）。
   - 展开「🔄 双 Agent 执行轨迹」，肉眼可见**第 1 轮：命题 Agent**（列出它发起的 RAG 检索 query 与出题数）→ **第 1 轮：校验评审 Agent**（逐题显示 ✅合格 / ❌不合格 + 原因）→ 若有不合格 → **第 2 轮：命题 Agent 重生成** → **第 2 轮：校验评审 Agent**，直到全部合格或达到 `EXAM_MAX_ITERATE`。
4. 点「⬇️ 导出 Markdown（含答案）」可下载试卷文件。

**证明能力**：
- **双 Agent 独立节点串联 + 迭代闭环**（绝非单次 LLM 调用）；
- **出题全部来自课件**（每道题带 `source_refs` 来源引用，轨迹里可见命题 Agent 主动发起多路 RAG 检索）；
- **不合格题重生成**（校验 Agent 逐题 4 项校验，fail 的题回传重生成）。

---

### 步骤 5：学生在线答题

**操作**：退出教师账号，用 `demo` / `Student@123` 登录，点导航「📝 试卷中心」→「✍️ 在线答题」标签页。选一份已就绪试卷，逐题作答（单选点选项、填空填文字、简答写段落），点「提交答卷」。

**前端现象**：
- 弹出「✅ 答卷已提交（ID xxx），客观题得分 X」。
- 出现进度条「主观题溯源批改中（RAG 检索课件 + 判分）…」。
- 完成后弹出「批改完成，总分 Y」，页面刷新可查看明细。

**证明能力**：学生（read）可在线作答提交；客观题**规则判分不调 LLM**、主观题后台异步批改、进度轮询。

---

### 步骤 6：学生查看批改结果（主观题附原文来源）★核心★

**操作**：学生登录后再次进入「在线答题」或直接刷新，查看自己答卷的批改明细。

**前端现象**：
- 展示「客观 X 分 + 主观 Y 分 = 总分 Z 分」。
- 每道客观题显示「✅ 正确 / ❌ 错误」。
- 每道简答题显示：
  - **得分 X / 满分 20**；
  - **✅ 优点**（答对的要点）；
  - **❌ 缺失/不足**（遗漏的知识点）；
  - **📖 课件原文溯源**（蓝色小字，逐条引用课件原文片段——即「这条评判依据来自课件哪句原文」）。

**证明能力**：主观题批改**附带原文来源（溯源，防幻觉）**，评判有据可查，不是大模型空口打分。

---

### 步骤 7：教师查看全班答卷与成绩

**操作**：退出，用 `admin` 登录，点「📝 试卷中心」→「📊 批改与成绩」标签页，选某份已就绪试卷。

**前端现象**：
- 显示「共 N 份答卷」。
- 每份答卷一条可展开卡片：学生昵称 + 总分 + 状态徽章（已批改/批改中）。
- 点开后展示该学生完整批改明细（含每道简答题的优点、缺失、课件原文溯源引用）。

**证明能力**：教师（write+）查看全班答卷 + 单份溯源批改详情；答卷数据全量落库。

---

### 步骤 8：权限隔离演示（RBAC + 答案不泄露）

**操作**：用 `demo`（学生）账号尝试越权操作。

**前端现象**：
1. 「试卷中心 → 试卷管理」中：**「生成试卷」表单不显示**（或提交被拦截提示「无生成试卷权限」）。
2. 试卷详情中：题目**不显示答案字段**、参考答案为空（后端已隐藏）。
3. 点「导出 Markdown」：下载的试卷**不含参考答案**（学生端强制去答案）。
4. 「批改与成绩」标签页：学生看不到「全班答卷」列表（后端 403）。

**证明能力**：四级 RBAC 落地到试卷全链路；read 学生禁止出卷、看不到答案、只能看自己的答卷。

---

### 步骤 9：审计日志查看

**操作**：用教师账号点导航「📊 审计日志」。

**前端现象**：
- 表格逐行展示刚才全部操作：登录、创建课程库、上传课件、**试卷生成任务（exam_generate）**、**学生提交答卷（exam_submit）**、**答卷批改（exam_grade）** 等，每条含时间、用户、操作名称、结果徽章。

**证明能力**：审计日志扩展覆盖课程创建 / 课件上传 / 出卷任务 / 答卷提交 / 批改。

---

## 二、测试要点（答辩必答，逐条演示）

### 要点 1：出题全部来自课件（不是大模型凭内部知识出题）

- **怎么证明**：生成试卷后展开「双 Agent 执行轨迹」，第 1 轮「命题 Agent」条目下方可见它**发起的多路 RAG 检索 query**（如「核心概念 定义 术语…」）；每道题详情带「📖 来源引用」，指向课件原文片段。
- **代码依据**：`generator_node.py` 先 `retrieve_material()` 检索课件原文，再把原文拼入 prompt 要求「严格基于课件原文出题，严禁编造知识点」；`validator_node.py` 逐题校验「①知识点真实存在于课件 ③不超纲」。

### 要点 2：不合格题目重生成（迭代闭环）

- **怎么证明**：轨迹里出现「校验评审 Agent」标记某题「❌ 不合格 + 原因（如：无法溯源）」→ 紧接「第 2 轮：命题 Agent」仅针对该题重生成 → 再校验，直到合格。`iterate_count` 记录实际迭代轮数。
- **代码依据**：`graph_builder.py` 的 `route_after_validator`：`rejected 非空 且 iterate < max_iterate → 回 generator`；`max_iterate` 读 `.env` 的 `EXAM_MAX_ITERATE` 硬上限防死循环。

### 要点 3：主观批改附带原文来源（溯源，防幻觉）

- **怎么证明**：学生简答题批改明细里，每条评判带「📖 课件原文溯源」蓝色引用。
- **代码依据**：`grader.py` 检索课件 → LLM 判分 → **溯源锚定**：LLM 返回的 source_refs 只有能匹配到课件原文才保留，编造引用一律丢弃；锚定为空回退到 top 检索片段。

### 要点 4：耗时操作全异步 + 可配置 + 向后兼容

- 上传课件、整套出卷、批改答卷三处均走 `submit_task` 后台任务，前端进度轮询，浏览器不冻结。
- 密钥/模型/开关全读 `.env`；`ENABLE_IMAGE_EMBED=false` 时系统正常（图片检索降级为空）。

---

## 三、答辩讲解要点速览

| 演示环节 | 一句话结论 |
|---------|-----------|
| 登录 → 课程库页 | JWT 鉴权 + 知识库→课程库语义改造 + tags 标签 |
| 建库 → 传课件 | 异步任务进度条（非阻塞）+ 分阶段状态可视化 |
| 生成试卷 + 轨迹 | **双 Agent 独立节点串联迭代闭环** + 出题带来源引用 |
| 学生答题 | 客观题规则判分（不调 LLM）+ 主观题异步批改 |
| 批改溯源 | 主观题评判**附课件原文来源**，防幻觉 |
| 教师看全班 | 答卷全量落库 + 单份溯源明细 |
| 权限隔离 | read 学生禁出卷、看不到答案、仅看自己答卷 |
| 审计 | 出卷/答卷/批改全留痕 |

---

## 四、常见问题排查

1. **出题/批改无结果** → 检查 `.env` 的 `DEEPSEEK_API_KEY` 是否真实 Key。
2. **课件状态停在「失败」** → 检查本地 BGE 嵌入模型是否可用（看后端日志）。
3. **试卷一直「生成中」** → 看后端日志，双 Agent 迭代可能未达上限；`EXAM_LLM_TIMEOUT` 过小会导致 LLM 超时。
4. **加成员报「用户不存在」** → 确认填的是用户 ID（学生 demo 通常为 2，可在审计日志确认）。
5. **端口冲突** → 换 `--port` 并同步 `.env` 的 `FRONTEND_API_BASE_URL`。

---

*配套演示素材见 `demo_docs/` 目录；本项目底层 RAG 引擎、LangGraph 框架、RBAC、异步任务、审计、OCR 均保留复用，仅上层业务与数据模型扩展。*
